{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node .claude/hooks/guards.mjs",
            "description": "Block running standard-version / pnpm release from the repo root (must run from app/)"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "node .claude/hooks/guards.mjs",
            "description": "Remind to keep the twin page extractors in sync and to treat manifest.json version/permission edits as a release concern"
          }
        ]
      }
    ]
  }
}
