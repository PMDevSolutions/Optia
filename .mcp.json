{
  "mcpServers": {
    "chrome-devtools": {
      "command": "cmd",
      "args": ["/c", "npx", "-y", "chrome-devtools-mcp@latest"],
      "description": "Chrome DevTools MCP — drive and screenshot the extension UI, run Lighthouse audits, inspect the DOM. The actual MCP this project uses (its tools are allow-listed in .claude/settings.local.json)."
    }
  }
}
